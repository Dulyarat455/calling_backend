BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Groups] ALTER COLUMN [createdAt] DATETIME2 NULL;
ALTER TABLE [dbo].[Groups] ALTER COLUMN [updateAt] DATETIME2 NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
